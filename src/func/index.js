

const BetweenTwoDays = require('./BetweenTwoDays')
const { SimpleImages, SimpleArrayPhotos, SimpleProfileInPosts } = require('./SimpleImages')
const Random = require('./Random')

const Func = {
    BetweenTwoDays,
    SimpleImages,
    SimpleArrayPhotos,
    Random,
    SimpleProfileInPosts,
}

module.exports = Func